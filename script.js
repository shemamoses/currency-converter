document.addEventListener("DOMContentLoaded", () => {
  const fromCurrencySelect = document.querySelector(
    'select[name="fromCurrency"]'
  );
  const toCurrencySelect = document.querySelector('select[name="toCurrency"]');
});
